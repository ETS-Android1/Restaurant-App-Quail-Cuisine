<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ewelionp.myrestaurantappquailcuisine">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRestaurantAppQuailCuisine">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key"/>

        <activity
            android:name=".DessertsActivity"
            android:exported="true"
            android:label="Menu / Desserts"
            android:theme="@style/Theme.CategoryMenu"/>
        <activity
            android:name=".MainCoursesActivity"
            android:exported="true"
            android:label="Menu / Main Courses"
            android:theme="@style/Theme.CategoryMenu"/>
        <activity
            android:name=".StartersActivity"
            android:exported="true"
            android:label="Menu / Starters"
            android:theme="@style/Theme.CategoryMenu"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>